Directory to store the ros packages and scripts required to build and run the traversal simulator. These packages are copied into the docker container where they are built using the catkin_make tool.
